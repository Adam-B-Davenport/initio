generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "sqlite"
    url      = "file:./initio.db"
}

model Character {
    id         Int    @id @default(autoincrement())
    name       String
    initiative Int
}
